version: "3.5"

services:
  bot: 
    build: .
    container_name: bot
    command: python bot/main.py
    volumes:
      - ./:/app


  form: 
    build: .
    container_name: form
    command: gunicorn -w 4 -b 0.0.0.0:8000 app_form.main:app
    volumes:
      - ./:/form
